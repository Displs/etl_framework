-- External-sink export DDL for ClickHouse.
-- Generated by etl-framework v{{ framework_version }} at {{ generated_at }}.
-- Source: {{ spec.target.fqn }}  -->  Sink: {{ sink.name }} ({{ sink.schema_ }}.{{ sink.table }})

CREATE TABLE IF NOT EXISTS {{ sink.schema_ }}.{{ sink.table }} (
{% for col in spec.mapping %}
    {{ col.target }} {{ col.type }}{% if not loop.last %},{% endif %}
{% endfor %}
)
ENGINE = MergeTree
ORDER BY ({% for k in spec.primary_keys %}{{ k }}{% if not loop.last %}, {% endif %}{% endfor %});
